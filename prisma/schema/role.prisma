model Role {
  id          Int     @id @default(autoincrement())
  name        String  @unique // e.g., "ADMIN", "MANAGER", "CASHIER"
  description String?

  users User[]

  created_at DateTime @default(now())
  updated_at DateTime @updatedAt

  @@map("roles")
}
